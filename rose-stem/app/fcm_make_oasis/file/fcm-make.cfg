$extract{?} = extract
$mirror{?} = mirror

steps = extract mirror

extract.ns = oa3mct
extract.location{primary}[oa3mct]=${OASIS_REPO_URL}
extract.location[oa3mct] =${OASIS_REPO_URL}@${OASIS_REV_NO}
extract.location{diff}[oa3mct]=${OASIS_SOURCES}

$ROSE_TASK_MIRROR_TARGET{?} =
mirror.target = ${ROSE_TASK_MIRROR_TARGET}
