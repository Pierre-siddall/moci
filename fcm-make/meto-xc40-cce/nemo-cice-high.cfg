include-path = $HERE/inc $HERE/../inc

$extract{?} = extract
$mirror{?} = mirror
$steplist{?} = $extract $mirror

$fcflags_level = -O3 -hvector3 -hscalar3 -hfp4 -haggress -hcache3 -hipa4 -hflex_mp=conservative -hzero

include = nemo-cice-common.cfg

build-ocean.prop{dep.o}[$nemo_ocean_src/$ocean_program.f90] = lib_cray 
