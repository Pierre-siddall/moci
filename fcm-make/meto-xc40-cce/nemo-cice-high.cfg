include-path = $HERE/inc $HERE/../inc

$extract{?} = extract
$mirror{?} = mirror
$steplist{?} = $extract $mirror

$fcflags_level = -O3 -Ovector3 -hfp0 -hflex_mp=strict -hipa3 -hlist=a

include = nemo-cice-common.cfg

build-ocean.prop{dep.o}[nemo/NEMOGCM/NEMO/OPA_SRC/$ocean_program.f90] = lib_cray 
